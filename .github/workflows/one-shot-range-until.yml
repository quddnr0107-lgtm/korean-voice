name: One-shot generic range until patch

on:
  push:
    branches:
      - gpt/range-until-suffix-20260912

permissions:
  contents: write

jobs:
  patch-and-verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Reproduce, patch, and verify
        run: node .github/scripts/patch-range-until.cjs
      - name: Commit verified patch and remove one-shot helpers
        shell: bash
        run: |
          rm -f .github/scripts/patch-range-until.cjs .github/workflows/one-shot-range-until.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'fix: keep range-until suffix on the right endpoint'
          git push origin HEAD:gpt/range-until-suffix-20260912
