name: One-shot legal collocation phrase patch

on:
  push:
    branches:
      - gpt/legal-daum-gat-20260912

permissions:
  contents: write

jobs:
  patch-and-verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Reproduce, patch, and verify
        run: node .github/scripts/patch-legal-daum-gat.cjs
      - name: Commit verified patch and remove one-shot helpers
        shell: bash
        run: |
          rm -f .github/scripts/patch-legal-daum-gat.cjs .github/workflows/one-shot-legal-daum-gat.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'fix: keep legal 다음과 같다 collocation in one phrase'
          git push origin HEAD:gpt/legal-daum-gat-20260912
